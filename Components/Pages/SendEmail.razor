@page "/send-email"
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "Admin")]
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/email-settings", true);
    }
}
