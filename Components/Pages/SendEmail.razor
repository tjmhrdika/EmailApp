@page "/send-email"
@using System.Net
@using System.Net.Mail
@using System.IdentityModel.Tokens.Jwt
@using System.Security.Claims
@using System.ComponentModel.DataAnnotations

@if (!string.IsNullOrEmpty(alertMessage))
{
    <div class="alert alert-@alertType alert-dismissible fade show" role="alert">
        @alertMessage
        <button type="button" class="btn-close" @onclick="() => alertMessage = String.Empty"></button>
    </div>
}

<div class="container mt-5">
    <div class="row g-4">

        <!-- LEFT -->
        <div class="col-md-5">
            <div class="card shadow-sm p-4 h-100">
                <EditForm Model="@setSmtp" OnValidSubmit="Set">
                    <DataAnnotationsValidator />

                    <h3 class="mb-3">Set SMTP</h3>
            
                    <div class="mb-3">
                        <label class="form-label">SMTP</label>
                        <InputText class="form-control" @bind-Value="setSmtp.Smtp" />
                        <ValidationMessage For="@(() => setSmtp.Smtp)" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <InputText type="email" class="form-control" @bind-Value="setSmtp.Email" />
                        <ValidationMessage For="@(() => setSmtp.Email)" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Password</label>
                        <InputText type="password" class="form-control" @bind-Value="setSmtp.Password" />
                        <ValidationMessage For="@(() => setSmtp.Password)" />
                    </div>

                    <div class="d-flex justify-content-end">
                        <button type="submit" class="btn btn-primary d-flex align-items-center gap-2">
                            <i class="bi bi-pencil"></i>
                            Edit
                        </button>
                    </div>
                </EditForm>
            </div>
        </div>

        <!-- RIGHT -->
        <div class="col-md-7">
            <div class="card shadow-sm p-4 h-100">
                <h3 class="mb-3">Test Email</h3>

                <div class="mb-3">
                    <label class="form-label">To Email</label>
                    <input type="email" class="form-control" @bind="toEmail">
                </div>

                <div class="mb-3">
                    <label class="form-label">Text</label>
                    <textarea type="email" class="form-control" rows="4" @bind="text"></textarea>
                </div>

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary d-flex align-items-center gap-2" @onclick=Send>
                        <i class="bi bi-send"></i>
                        Send
                    </button>
                </div>
            </div>
        </div>

    </div>
</div>

@code {
    SetSmtp setSmtp = new();
    string toEmail = String.Empty;
    string text = String.Empty;
    string alertMessage = String.Empty;
    string alertType = String.Empty;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            string? token = await JS.InvokeAsync<string>("localStorage.getItem", "authToken");
            if (string.IsNullOrEmpty(token))
            {
                NavigationManager.NavigateTo("/");
                return;
            }

            JwtSecurityTokenHandler? handler = new JwtSecurityTokenHandler();
            JwtSecurityToken? jwt = handler.ReadJwtToken(token);
            string? role = jwt.Claims.FirstOrDefault(c => c.Type == ClaimTypes.Role)?.Value;
            if (role != "Admin")
            {
                NavigationManager.NavigateTo("/");
            }
        }
    }

    protected override void OnInitialized()
    {
        setSmtp = db.SetSmtp.First();
    }

    async Task Set()
    {
        db.SetSmtp.Update(setSmtp);
        await db.SaveChangesAsync();
    }

    void Send()
    {
        try
        {
            SetSmtp currentSetSmtp = db.SetSmtp.First();
            
            SmtpClient? smtp = new SmtpClient(currentSetSmtp.Smtp)
            {
                Port = 587,
                Credentials = new NetworkCredential(
                    currentSetSmtp.Email,
                    currentSetSmtp.Password
                ),
                EnableSsl = true
            };

            MailMessage? mail = new MailMessage
            {
                From = new MailAddress(currentSetSmtp.Email),
                Subject = "Alarm Notification",
                Body = text,
            };

            mail.To.Add(toEmail);
            smtp.Send(mail);

            alertMessage = "Email sent!";
            alertType = "success";
        }
        catch (Exception ex)
        {
            alertMessage = ex.Message;
            alertType = "danger";
        }
    }
}